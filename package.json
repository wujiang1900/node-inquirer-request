{
  "name": "node-inquirer-request",
  "version": "1.0.0",
  "description": "a simple node program using inquirer and request packages",
  "main": "src/index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "npm run build && nodemon dist/index.js",
    "serve": "npm run build && node dist/index.js",
    "test": "npm run build && mocha --require babel-register"
  },
  "author": "John Wu<wujiang1900@yahoo.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.17.5"
  },
  "dependencies": {
    "inquirer": "^6.2.2",
    "request": "^2.88.0",
    "underscore": "^1.9.1"
  }
}
